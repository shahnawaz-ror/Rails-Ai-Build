<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Rails AI Build IDE" %></title>
  <%= render "rails_ai_build/shared/theme" %>
  <%= yield :head %>
</head>
<body class="rab-ide-body">
  <%= yield %>
  <%= yield :scripts %>
</body>
</html>
