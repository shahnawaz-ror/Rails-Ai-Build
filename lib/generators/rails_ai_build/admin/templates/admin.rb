# frozen_string_literal: true

# Admin panel configuration for Rails AI Build
RailsAiBuild.configure do |config|
  # Enable diff preview for admin panel (requires Pro plan or higher)
  # config.diff_preview = true
  # config.plan = :pro

  # Enable audit logging for team visibility (requires Team plan)
  # config.audit_enabled = true
  # config.plan = :team
end
